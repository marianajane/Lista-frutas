function agregarFruta() {
    var frutas = [manzana, naranja];

    this.add = add;
    this.size = size;

    function add(element) {
      document.getElementById frutas.push(frutas);
    }

    function size() {
      document.getElementById (frutas.length);
    }
}
